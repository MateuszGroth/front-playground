export const setBarFill = (thresholdList: any[], isDarkMode: boolean, isGradient: boolean) => {
  // set fill based on threshold and flags
  return '#fff'
}

export const styleTooltip = (tooltipGroup: any, tooltipData: any, tooltipLeft: number) => {
  // apply tooltip styles
}

export const moveXAxisLabels = (xAxisLabels: any, xAxisLabelWidth: number) => {
  // adjust vertically
}

export const getYTicks = (height: number) => {
  // set y axis ticks based on height
}

export const removeYAxisVerticalLine = (selection: any) => {
  // remove axis line
}

export const moveYAxisValuesOnTopOfGridLines = (letterWidth: number, unit: string) => (group: any) => {
  // ajust y axis labels vertically and horizontally
}
